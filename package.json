{
  "name": "power-outage-widget",
  "version": "1.0.0",
  "description": "API for power outage widget",
  "main": "scripts/server.js",
  "scripts": {
    "start": "node scripts/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}