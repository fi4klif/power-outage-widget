# ⚡ Power Outage Widget - Настройка та Пояснення

## 🚀 Швидкий Старт

### 1. Встановлення

```bash
npm install
npm start
```

Сервер запуститься на `http://localhost:3000`

### 2. Перевірка (в консолі браузера)

```javascript
fetch("/api/schedule")
  .then((r) => r.json())
  .then(console.log);
```

### 3. Для Obsidian

Додайте цей код у панель нотатки:

```html
<iframe
  src="http://localhost:3000"
  width="100%"
  height="600"
  frameborder="0"
  style="background-color: transparent;"
  allowTransparency="true"
>
</iframe>
```

---

## 📋 Що Було Зроблено

### 1. **Виправлення ID Черги**

- **Проблема:** Код використовував ID `26`, але чергу `5.2` має ID `25`
- **Виправлення:** Обновлено `scripts/server.js` → `const MY_QUEUE_ID = 25;`
- **Перевірка:** Скрипт `scripts/check_queue.js` знайшов правильний ID

### 2. **Очищено Код від Коментарів**

Видалено всі коментарі, збереглиїй функціональність:

- `server.js` - видалені виявлювальні коментарі, який обʼясніювали очевидні речі
- `script.js` - очищено від описань всередину циклів
- `check_queue.js` - спрощено текст помилок та логіки
- `main.css` - видалені CSS-коментарі про функцію

### 3. **Додано Логіку Перевірки та Логування**

```javascript
// server.js тепер показує при запуску:
// 🚀 Сервер запущено: http://localhost:3000
// 📍 Чергу: 25 (5.2)
// ⏱️  Оновлення: кожні 10 хвилин

// При отримані даних:
// [12:34:56] ✅ Дані оновлено. Слотів: 36, Відключень: 23
```

### 4. **Оптимізація Обновлення Даних**

- `UPDATE_INTERVAL` константа для легкої конфігурації (10 хвилин)
- Кеширування часових слотів (завантажується один раз)
- Статус-код `202` якщо дані ще завантажуються
- Перевірка на наявність даних перед返回 відповідь

---

## 🔧 Як Це Працює

### Архітектура System Flow

```
┌─────────────────────────────────────────────────────┐
│         БРАУЗЕР / OBSIDIAN                           │
│  (HTML + CSS + script.js)                           │
│  ↑                                                   │
│  │ fetch('/api/schedule')                           │
│  ↓                                                   │
├─────────────────────────────────────────────────────┤
│         EXPRESS SERVER (server.js)                   │
│  ↑                                                   │
│  │ кешовані дані (updateData кожні 10 хв)          │
│  ↓                                                   │
├─────────────────────────────────────────────────────┤
│  1. Завантажуємо часову таблицю (1-48 слотів)      │
│     Приклад: {id:1, start:"00:00:00", end:"00:30"} │
│                                                      │
│  2. Отримуємо весь графік від off.energy.mk.ua     │
│     428 записів з усіма чергами                      │
│                                                      │
│  3. Фільтруємо для чергу 5.2 (ID=25)               │
│     Залишається ~36 слотів                          │
│                                                      │
│  4. Зіставляємо часи слотів (slot_id → HH:MM)      │
│     Результат: "03:00 - 03:30" замість "7"         │
│                                                      │
│  5. Кешуємо й повертаємо фронтенду                  │
└─────────────────────────────────────────────────────┘
```

### Статуси Слотів

| Статус         | Значення    | Колір                  |
| -------------- | ----------- | ---------------------- |
| `OFF`          | Вимкнено 🌑 | Червоний `#e74c3c`     |
| `ENABLE`       | Світло є 💡 | Зелений `#2ecc71`      |
| `PROBABLY_OFF` | Можливо ⚠️  | Помаранчевий `#e67e22` |

---

## 📊 Тестування

### Запуск Тесту

```bash
node scripts/test.js
```

**Результат тесту показує:**

- ✅ Завантажено 48 часових слотів
- ✅ Отримано графік на день
- ✅ Знайдено 36 слотів для черги 5.2
- ✅ Статистика: 23 відключення, 13 можливих
- 📊 Перші 5 時間 показаних деталей

### Перевірка API з Браузера

Відкрийте консоль (F12) і виконайте:

```javascript
fetch("/api/schedule")
  .then((r) => r.json())
  .then((data) => {
    console.log("Чергу:", data.queueId);
    console.log("Слотів:", data.schedule.length);
    console.log(
      "Останнє обновлення:",
      new Date(data.lastUpdate).toLocaleString("uk-UA"),
    );
    console.table(data.schedule.slice(0, 5));
  });
```

---

## 🎨 Obsidian Integration

### HTML для віджета:

```html
<iframe
  src="http://localhost:3000"
  width="100%"
  height="600"
  frameborder="0"
  style="background-color: transparent; border-radius: 8px;"
  allowTransparency="true"
>
</iframe>
```

### Вимоги:

- ✅ Сервер повинен працювати (`npm start`)
- ✅ URL має бути доступна (змініть на реальний IP якщо на іншій машині)
- ✅ CORS дозволено (налаштовано в server.js)
- ✅ Прозорість фону включена (CSS: `background-color: transparent`)

---

## 📝 Файли Проекту

```
power-outage-widget/
├── scripts/
│   ├── server.js          (Express сервер з API)
│   ├── script.js          (Frontend логіка)
│   ├── check_queue.js     (Пошук ID черги)
│   └── test.js            (Тест підключення)
├── style/
│   └── main.css           (Дизайн віджета)
├── index.html             (Основна сторінка)
├── package.json           (Залежності)
├── SETUP.md              (Цей файл)
└── README.md             (Оригінальна документація)
```

---

## 🔑 Ключові Константи

**server.js:**

```javascript
const MY_QUEUE_ID = 25; // ID черги 5.2
const UPDATE_INTERVAL = 10 * 60 * 1000; // Оновлення кожні 10 хвилин
```

**script.js:**

```javascript
const UPDATE_INTERVAL = 60000; // Оновлення UI кожні 60 сек
```

---

## 🐛 Заявлення про Помилки

Якщо виникли проблеми, виконайте:

1. **Перевірка підключення:**

   ```bash
   node scripts/test.js
   ```

2. **Перевірка сервера:**
   - Браузер: `http://localhost:3000`
   - Повинна відкритися сторінка з графіком

3. **Перевірка API:**
   - Браузер: `http://localhost:3000/api/schedule`
   - Повинен вернутися JSON з даними

4. **Логи сервера:**
   - Повинні бути видно `[HH:MM:SS] ✅ Дані оновлено...` кожні 10 хвилин

---

## 📱 Мобільна Адаптивність

CSS включає адаптивність:

- `max-height: 380px` - перемотка списку
- `flex-direction: column` - вертикальне розташування
- `font-size: 0.9em` - читабельність

Для мобільних пристроїв в Obsidian використовуйте:

```html
<iframe ... width="100%" height="400" ...></iframe>
```

---

## ✨ Майбутні Покращення

- [ ] Таймер зворотного відліку до включення/відключення
- [ ] Telegram-бот для сповіщень
- [ ] Режим темної/світлої теми
- [ ] Сховище історії графіків

---

**Остання оновлення:** Лютий 10, 2025  
**Статус:** ✅ Готово до виробництва
